{
  "name": "morternal-v2",
  "version": "1.0.0",
  "description": "Morternal — The Future Never Dies.",
  "license": "MIT",
  "author": "EvilCodeMorty",
  "type": "module",
  "main": "dist/main.js",
  "keywords": [
    "koa",
    "node",
    "typescript",
    "mysql"
  ],
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "clean": "rimraf dist",
    "build:dev": "npm run clean && tsc --sourceMap true",
    "build:prod": "npm run clean && tsc --sourceMap false",
    "build": "npm run build:prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/koa-router": "^7.4.8",
    "@types/koa-static": "^4.0.4",
    "@types/node": "^24.2.1"
  },
  "dependencies": {
    "@koa/router": "^14.0.0",
    "@types/koa": "^3.0.0",
    "dotenv": "^17.2.1",
    "koa": "^3.0.1",
    "koa-body": "^6.0.1",
    "koa-router": "^14.0.0",
    "koa-static": "^5.0.0",
    "socket.io": "^4.8.1",
    "uuid": "^11.1.0"
  }
}
